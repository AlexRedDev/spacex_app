class StarshipAPI {
  Future<Map<String, dynamic>> fetchAllStarships() async {
    throw UnimplementedError;
  }

  Future<Map<String, dynamic>> fetchStarshipsBy({required int id}) async {
    throw UnimplementedError;
  }
}
