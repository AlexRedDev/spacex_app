import 'package:flutter/material.dart';

class StarshipScreen extends StatelessWidget {
  const StarshipScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Starship')),
    );
  }
}
